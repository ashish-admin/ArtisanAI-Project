// Path: frontend/pubspec.yaml

name: artisan_ai
description: "A new Flutter project."
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=3.5.0-151.0.dev <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  provider: ^6.1.2
  dio: ^5.4.3+1
  flutter_secure_storage: ^9.2.2
  google_fonts: ^6.2.1

  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true